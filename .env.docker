# Docker Environment Configuration
# Copia este archivo a .env y ajusta los valores según tu entorno

# ====================
# PostgreSQL Database
# ====================
POSTGRES_USER=creapolis
POSTGRES_PASSWORD=creapolis_password_2024
POSTGRES_DB=creapolis_db
POSTGRES_PORT=5432

# ====================
# Backend API
# ====================
NODE_ENV=production
BACKEND_PORT=3000

# Database Connection (usado por el backend)
DATABASE_URL=postgresql://creapolis:creapolis_password_2024@postgres:5432/creapolis_db?schema=public

# ====================
# JWT Configuration
# ====================
JWT_SECRET=your-super-secret-jwt-key-CHANGE-THIS-IN-PRODUCTION-use-openssl-rand-base64-32
JWT_EXPIRES_IN=7d

# ====================
# Google OAuth (Opcional)
# ====================
GOOGLE_CLIENT_ID=your-google-client-id-from-console
GOOGLE_CLIENT_SECRET=your-google-client-secret-from-console
GOOGLE_REDIRECT_URI=http://localhost:3000/api/integrations/google/callback

# ====================
# CORS Configuration
# ====================
# Múltiples orígenes separados por coma
# Ejemplos: Vite frontend (5173), Flutter web (8080), React Native web, etc.
CORS_ORIGIN=http://localhost:5173

# ====================
# Rate Limiting
# ====================
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# ====================
# PgAdmin (Opcional)
# ====================
PGADMIN_EMAIL=admin@creapolis.com
PGADMIN_PASSWORD=admin
PGADMIN_PORT=5050
